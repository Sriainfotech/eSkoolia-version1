// Compatibility placeholder:
// this workspace uses Django as the source of truth, and student-group APIs are proxied.
// Keep this export to satisfy module expectations from the shared implementation guide.
export const prisma = null;
