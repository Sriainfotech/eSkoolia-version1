# This file is kept so every existing `pip install -r requirements.txt`
# invocation (docs, setup.bat, docker-compose.yml, scripts/) keeps working
# unchanged. It installs the full dev set — the same packages this file
# used to list directly.
#
# Production/CI installs should use requirements/prod.txt or
# requirements/dev.txt directly instead of this file. See
# requirements/base.txt, requirements/dev.txt, requirements/prod.txt.
-r requirements/dev.txt
